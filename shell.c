/**
 * main - check the code
 * @a_c: The number of arguments supplied to the program.
 * @a_v: An array of pointers to the arguments.
 * Return: Always 0.
 */
int main(int a_c, char *a_v[])
{
return (0);
}
